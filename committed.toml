style = "conventional"
subject_capitalized = false
# Reject merge commits in history (this repo rebases/fast-forwards
# instead). CI checks out the PR branch's actual head commit rather
# than GitHub's synthetic merge ref, so this never lints GitHub's own
# auto-generated merge commit message - see .github/workflows/ci.yml.
merge_commit = false
